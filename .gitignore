# Secrets and local overrides (never commit)
.env
config.local.json

# Win32 Content Prep output and per-run scratch
staging/
*.intunewin

# Processed installers and rejection queue (may contain binaries)
done/
failed/

# Test output
testResults.xml

# Local debug NDJSON (agent triage)
debug-7596d3.log

# OS / editor
Thumbs.db
.DS_Store
*.swp
*.swo
